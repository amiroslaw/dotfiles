#!/usr/bin/env bash
# https://github.com/svenstaro/rofi-calc
# I can't setup
rofi udiskie-dmenu -matching regex -dmenu -i -no-custom -multi-select
# rofi -show udiskie-dmenu -matching regex -dmenu -i -no-custom -multi-select
# rofi -show calc -modi calc -no-show-match -no-sort
